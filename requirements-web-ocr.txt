# Minimal conversion pipeline for online OCR backends such as Baidu OCR.
#
# This keeps OCR model stacks out of the environment. It is enough for:
#   DECKWEAVER_OCR_BACKEND=baidu
#   scripts/convert.py --ocr-backend baidu
python-pptx
pillow
numpy
opencv-python-headless
pymupdf
